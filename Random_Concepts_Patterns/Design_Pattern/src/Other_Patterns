Factory Pattern- Class creational pattern.

It externalize object creation and allows subclasses to decide the type of object

State Pattern- similar to strategy pattern

It handles state of an object.

Compound Pattern - combines two or more patterns to solve a recurring problem

Example : Model View Controller(MVC) Pattern 
View - Composite pattern
Model - Observer pattern
Controller - Strategy pattern